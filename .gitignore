# Payroll123 has its own repository — only the empty directory placeholder is tracked here
payroll123/*
!payroll123/.gitkeep

# Azure DevOps PoC reference (local only)
yaml_azure.yaml

# Docusaurus
website/node_modules/
website/build/
website/.docusaurus/
